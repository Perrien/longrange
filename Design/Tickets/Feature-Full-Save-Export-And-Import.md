# Full Save Export & Import

Status: open
Filed: 2026-08-13

Export and re-import the complete save — rifle instances, ammo lots, confirmed nodes, trued parameters
and progression — as JSON, so a second device reproduces the data book exactly. Today's export and
import only cover what schema v2 already carried.
