# Environment textures

ambientCG assets (Grass004, Ground082S, Bark012, Rock030), CC0 / public domain —
https://ambientcg.com

Copied from `BallisticsToolkit/web/textures/` (the pristine oracle repo) for the
Test Range environment module (`src/range/environment/`). Precached by the PWA
service worker for offline play — see `vite.config.ts`'s Workbox `globPatterns`.
